{% extends 'base.html.twig' %}

{% block title %}Lavori{% endblock %}

{% block body %}
    <h1>Lavori</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lavori.id }}</td>
            </tr>
            <tr>
                <th>Specialista</th>
                <td>{{ lavori.specialista }}</td>
            </tr>
            <tr>
                <th>Intervento</th>
                <td>{{ lavori.intervento }}</td>
            </tr>
            <tr>
                <th>Prezzo</th>
                <td>{{ lavori.prezzo }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ lavori.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lavori_index') }}">back to list</a>

    <a href="{{ path('app_lavori_edit', {'id': lavori.id}) }}">edit</a>

    {{ include('lavori/_delete_form.html.twig') }}
{% endblock %}
